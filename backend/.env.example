# market_data_dwndr backend configuration.
# Copy to `.env` and fill in. `.env` is gitignored -- never commit secrets.
# See docs/60-operations/config-and-env.md.

# --- required ---
KITE_API_KEY=your_api_key
KITE_API_SECRET=your_api_secret
MARKET_DATA_PATH=/data/MARKET_DATA

# --- optional (locked defaults shown) ---
INDICES=NIFTY,BANKNIFTY,FINNIFTY,SENSEX
STOCK_UNIVERSE=all
CAPTURE_HZ=1
ZSTD_LEVEL=17
MARKET_OPEN=09:15
MARKET_CLOSE=15:30
TIMEZONE=Asia/Kolkata
LOG_LEVEL=INFO
HTTP_PORT=8000

# NOTE: the daily Kite access_token and the 10-yr bond yield are NOT set here.
# They are entered at morning login and stored in session state
# (see docs/60-operations/session-state.md).
